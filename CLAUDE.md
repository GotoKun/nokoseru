# AI Hackathon2026 - プロジェクト指示・経緯まとめ

このファイルは、プロジェクト設定（`metadata.json`の思想）を踏まえた上で、実際の調査・議論で得られた結論と、今後このプロジェクトを引き継ぐ際に踏まえるべき指示をまとめたものです。プロジェクトの基本思想（AIから考えない、既存の取り組みを疑う、AI基盤企業に吸収される機能を避ける等）は元のプロジェクト設定を参照し、ここでは会話で積み上がった具体的な結論のみを記録します。

---

## 追加ファイル（2026-08-13）

以下2ファイルを追加。

- `nokoseru_planning.md` — 企画概要（ノコセル：AIが高齢者にインタビューを行い映像で記録し、指定した節目で家族に配信するサービス。終活・葬儀業界向け）
- `research_report.md` — 市場・競合・倫理 調査レポート（出典付き。HereAfter AI閉鎖／StoryFile破産／Remento・Storii等の海外先行例／Revibot等の国内葬儀社チャネル先行例／ケンブリッジ論文の倫理指針などを整理）

これ以降の本ファイル本体（下記セクション群）は教育領域での初期調査・議論の経緯記録。現在の検討中心は上記2ファイルのノコセル企画に移っている点に注意して引き継ぐこと。

---

## 実装方針の確定事項（2026-08-13、相談で固まった内容）

`nokoseru_planning.md` 策定後の相談で追加確定した技術・事業設計。実装時はこの内容を前提とすること。

### 買い切り×ストレージコストの整合性

- AWS Glacier Deep Archive相当（約$0.00099/GB・月）を使えば、1人あたりの生涯収録量（想定50〜100GB）のストレージ原価は30年で¥5,000〜1万円程度。一時金¥3〜5万円で十分吸収できる。「Webアプリ＝サブスク必須」という前提は誤り。
- 悪用（意図的な連投等）によるコスト膨張は、ハッカソンのスコープでは対応不要と判断（本番運用時は要検討）。対策の方向性としては、ユーザーに見える上限は設けず（7.4の「進捗・上限を可視化しない」思想を維持）、裏側の異常検知・フェアユース条項で対応する案が出ている。
- 3層ストレージ（温層→アクセス減少後にコールドアーカイブへ自動降格、節目イベント接近時は事前復元）を採用する。
- 会社の存続リスク（HereAfter AI・StoryFileの実例）はストレージ原価の問題ではなく固定費（人件費等）の問題。対策はエクスポート機能をオプションではなく配信フローの必須ステップにすること。

### エクスポート／継続性の設計

- エクスポートは「アプリ化してLLMごと持たせる」方式ではない（重すぎるため不採用）。LLMの仕事は収録時（質問生成・構造化）に一度だけ発生し、その**出力結果**（書き起こし・タグ・エピソード情報）だけを持ち出せればよい。
- エクスポート実体：動画ファイル＋字幕ファイル（SRT）＋タグ・エピソード情報のJSON。サーバー・LLM不要で動画プレイヤーとテキストだけで開ける。
- 検索性を残す場合は、JSONを読み込んでクライアント側でキーワード・タグ絞り込みするだけの自己完結型HTML（サーバー・LLM不要）を同梱する。
- 配信のたびにこのエクスポート一式を家族に渡す（ブラウザダウンロードや物理メディアでも可）。サービスが終了しても中身は手元に残る。

### AIの役割の切り分け（収録側と閲覧側）

- **収録側（要LLM・要OrcaRouter）**：インタビュー質問の動的生成、収録内容の構造化（エピソード分割・タグ付け）、未収録領域の検出。
- **閲覧側（LLM不要）**：視聴者の質問を既存のタグ付き動画にマッチさせて、該当箇所へシーク再生するだけ。生成・再合成は行わない。マッチングはキーワード・タグの単純一致で足りる。
- この切り分けにより、閲覧側はオフライン・ローカルでも動作し、エクスポート後もサービス終了後も機能が保たれる。
- ハッカソンデモでは、この切り分けを活かし、閲覧側の実装をキーワードマッチに絞ることでLLM呼び出しをデモ中の質問応答から排除し、安定性を上げつつ「AIである必然性」を収録側に集約して見せる。ただし本番の質問は固定リストではなく動的生成が前提（6.1）なので、デモでは想定問答に絞っている旨を提出物の説明に明記すること。

### 未確定・要検討事項

- ~~OrcaRouterのルーティングモード選定~~ → `nokoseru_design.md`で確定（4.4節）。STTはGeminiのマルチモーダル音声入力で解決、構造化はStructured Outputsで安定化。
- 永代供養料（一時金で無期限のサービスを担保する同業界の先例）をピッチのロジックとして使う案があるが、準備金の実装自体は本番運用時の検討課題でハッカソン範囲外。

### 設計書

`nokoseru_design.md`（2026-08-13追加）— 実装着手用の技術設計書。技術スタック案、OrcaRouter利用設計（公式ドキュメント確認済み、STT含む）、データモデル、画面・API一覧、エクスポート仕様、実装スケジュールをまとめたもの。Claude Codeでの実装はこのファイルを起点にする。

### wow機能：写真カラー化＋写真ベース質問生成（2026-08-13追加）

- 音声・映像の生成（リップシンク・声のトレース）は却下。技術難易度・既存サービスとの倫理的近さ（Revibot型）を理由にチームで不採用と判断。AIは引き続き裏側（質問生成・構造化）のみで使う方針を再確認。
- 代わりに、白黒写真のカラー化（OrcaRouterの画像編集モデル経由）＋その写真を踏まえた質問生成を採用。詳細設計は`nokoseru_design.md`4.5節・11章。
- 写真カラー化は葬儀業界（カメラのキタムラ、みづま等）で既にコモディティ化した機能のため、単体では差別化にならない。ピッチでは「カラー化×写真ベース質問生成」の組み合わせとして説明すること。
- サービス全体の位置づけを「終活サポートアプリ」（生前に本人が使い、何を撮るべきかをアプリが提案、動画をストックして節目に配信）として言語化。既存の企画書の中身とは矛盾しない再確認。

---

## 現在地：調査で固まった事実

### 課題として一次資料で裏付けが取れているもの

1. **目的意識の希薄な進学**：文科省「キャリア教育の推進に関する総合的調査研究協力者会議報告書」（平成16年）が「進路意識や目的意識が希薄なまま『とりあえず』進学する若者の増加」を公式に明記。約20年前から国が認識している課題。
2. **探究学習の形骸化**：令和7年10月15日の教育課程部会資料（最新）が、評価方法の不明確さ・教員負担の偏り・生徒個人の興味関心の軽視・ICT活用の国際的な遅れ（PISA2022でOECD29カ国中29位）を明示。高校教員の9割超が課題を感じている（カタリバ2023調査）。
3. **産業界と教育現場の接続不足**：経産省「未来人材ビジョン」（2022年）が政策課題として明記。

詳細と出典は `教育課題_一次資料調査.md` を参照。

### 「目的意識の希薄な進学」と「探究の形骸化」の統合仮説について

国の資料が直接この2つを結びつけているわけではない。AOI Research（2023）のデータ（生徒が自分でテーマを決めた探究がキャリア形成に影響を与える傾向）が部分的に支持するのみ。**因果関係の実証ではなく、複数資料を横断した仮説**として扱うこと。

---

## 重要な転換点：「答えを出さないAI」路線を却下した経緯

当初、探究学習支援として「答えを出さないAI（Thinking AI）」という方向性を検討していたが、以下の理由で**この路線単体では採用しない**と判断した。

1. **すでに国レベルで実証済み**：文部科学省委託の生成AI実証事業（2025年度、2026年3月成果報告、TBSホールディングス・東京書籍等5社、渋谷区等13校）が、会話戦略エージェントによる「直接回答しない」設計を実装済み。8割以上の児童生徒が「深く考えられた」と回答という結果も出ている。出典：[成果報告書](https://www.mext.go.jp/content/20260514-mxt_shuukyo01-000042009_003.pdf)
2. **模倣コストがほぼゼロ**：この機能の実体は「システムプロンプトで探究学習のプロセスを設定」「直接回答しない会話戦略エージェント」であり、LLMへの振る舞い指示（プロンプトエンジニアリング）に過ぎない。技術的な参入障壁が極めて低く、AI基盤企業や競合が即座に模倣できる。
3. **報告書自身が課題として未解決と認めている**：「答えない」ことへの児童生徒のストレス・離脱（検索ツールへの逃避）が報告されており、適切な情報提示度合いの最適解は得られていない。

**教訓**：「〜するAI」という機能・振る舞いのフレーズで発想すると、プロンプト工夫の域を出ず、事業としての独自性が弱い。今後のアイデア出しでは、この種のフレーズ（例：「答えを出さないAI」「寄り添うAI」等の機能修飾語ベースの発想）を避け、下記の3軸で考える。

---

## 独自性を作る3つの軸（プロンプト工夫と区別するための基準）

新しいアイデアを評価する際は、必ず以下のどれに該当するかを確認する。どれにも該当しない場合は「プロンプトの工夫」に留まっている可能性が高いと疑う。

1. **時間軸で積み上がる構造**：複数年にわたるデータ・関係性の蓄積など、後発が同じプロンプトを書いても即座に再現できないもの。
2. **評価の構造化**：正解のない学びの質を定量・準定量化する仕組みなど、対話生成そのものより難易度が高い技術的課題。
3. **バラバラなデータソースをつなぐ設計**：現状分断されているシステム・データ（探究データ、キャリアデータ、企業データ等）を構造的に接続する、ソフトウェアエンジニアリング的難易度を伴うもの。

## 白地領域（未解決だが理由の性質を要区別）

文科省実証事業・既存EdTechサービス（スクールAI、TimeTact、都立AI等）を横断して確認した未解決領域は以下。ただし「技術的に誰も作っていないだけ」なのか「制度・専門性の壁で構造的に解けない」のかを区別すること（後者にAIをぶつけても解決しない）。

- 学年をまたぐ継続的な対話の蓄積（→ 個人情報・自治体セキュリティ基準という制度的な壁が強い）
- 探究とキャリア形成の接続（→ 都立AIとTimeTact/クエストエデュケーションの間に統合例なし）
- 「答えない」ことへの心理的ストレスの解消策（→ 技術・UXの未解決課題として報告書に明記）
- 興味関心と学習内容の精緻なマッチング（→ 文科省実証・TimeTact双方で試みるも機能せず）
- 評価・記録の教員負担軽減（→ 「今後の検討課題」止まりで未実装）
- 芸術・美術・哲学など感性を扱う正解のない学び（→ 文科省実証の対象教科外）

詳細と出典は `教育課題_AI活用の既存事例と機会領域.md` を参照。

---

## 検討中のアイデアの種（2026年8月12日時点、未確定）

いずれも「機能のフレーズ」ではなく「軸のどれに当たるか」で評価すること。

| 案 | 該当する軸 | 弱点・懸念 |
|---|---|---|
| 複数年の興味関心ログを構造化して伴走 | 軸1 | 個人情報・自治体間セキュリティ基準の壁。ハッカソンでは疑似データでのデモになる |
| 探究プロセスの深まりを構造的にスコア化 | 軸2 | 「思考の深まり」の指標が教育学的に未確立。専門家監修なしでは妥当性に疑義 |
| 探究の興味関心ログ×企業データのマッチング | 軸3 | 企業データの取得・提携がハッカソン期間内に終わらない。BizDev的難易度 |
| 生徒の探究ログを進路面談用に構造化提示 | 軸2寄り | 教員支援ツールとしては地味で、デモ映えしにくい |

軸1×軸3の組み合わせ（複数年の興味関心ログ×企業データのマッチング）も候補として言及されたが未深掘り。

---

## 次回セッションへの申し送り

1. アイデア出しの際は、必ず上記3軸のどれに当たるかをまず確認してから議論を始める
2. 「〜するAI」という機能フレーズでの発想は避ける
3. 白地領域は「技術的空白」か「制度的な壁」かを区別してから機会評価する
4. 非公開の類似サービス・特許の重複確認をハッカソン提出前に必ず行う
5. ビジネスモデル（誰が・なぜ払うか）はまだ未着手。次の検討ステップ

---

## ハッカソン運営情報（AI HACK 2026）

### 基本情報

- ハッシュタグ：`#AIHACK` `#OrcaRouter`
- テーマ：「本番で通用する次世代のAI Productを作る！」
- 運営：こまさん（高野さん）－ キャリア形成・独立支援の文脈に関心を持つ方

### スポンサー

- OrcaRouter（**利用必須**、詳細は下記）
- まじめちゃん転職
- 株式会社WHERE
- 株式会社Levela（畑来（はたら）さん／AI活用ラボ@Levela、Xコンサル・顧問）

### 審査

審査員による座談会が8/16に予定されている。審査基準はエンジニア観点・ビジネス観点の両方から構成され、以下8項目を各1〜5点で採点。

1. 課題の実在性
2. ビジネスの成立性
3. 完成度／デモの説得力
4. AIである必然性
5. 技術的な作り込み
6. LLMコスト
7. セキュリティ
8. 次世代性／独創性

→ このプロジェクトが重視してきた「プロンプト工夫で終わらない独自性（3軸）」は、審査基準の4（AIである必然性）・5（技術的な作り込み）・8（次世代性/独創性）と直接対応する。

→ 6（LLMコスト）・7（セキュリティ）については、スポンサーであるOrcaRouterのツールを利用すればカバーされる設計になっている。この2項目が審査基準に含まれているのは、スポンサーツールの利用を各チームに促す狙いがあると考えられる。したがって、コスト・セキュリティ面で独自に作り込む必要性は薄く、OrcaRouterを組み込むこと自体がこの2項目への対応になる。

審査の流れ：課題提出 → ブース審査（全チーム対象）→ 最終プレゼン（4分プレゼン＋3分質疑応答、上位8チームのみ）

賞金：最優秀賞60万円／優秀賞30万円／スポンサー賞10万円

### OrcaRouter（利用必須の技術要件）

LLMルーター。1つのAPIで200以上のモデルを利用可能。ルーティングモードが4種類あり、自社LLM・最新モデルいずれも利用できる。LLMコストとセキュリティの観点で強みがあるとされ、開発の土台として使うことが前提。

- バウチャー付きURL：https://www.orcarouter.ai/redeem/AI-HACK-2026-ORCAROUTER
- 公式ドキュメント：https://docs.orcarouter.ai/ja/introduction

**注意：OrcaRouterの利用がレギュレーション上必須。** アイデア設計・技術選定の際は、OrcaRouter経由でのLLM呼び出しを前提にアーキテクチャを組む必要がある。

### 最終成果物・提出物（締切 8/15 15:00、Googleフォーム提出）

- GitHubリポジトリ（コードを共有）
- デモ動画（3分以内）
- Qiita または Zenn の記事（ハッカソンでの活動・開発したプロダクトが分かる内容）

### レギュレーション

- 既存コードの使用は可
- 指定ツール（OrcaRouター等）の利用ルールに注意
- テーマに沿った内容にすること
- API Keyの流出に注意し、共有前に必ず確認すること

### 最終発表会

- 日時：8/16（日）8:50〜
- 場所：渋谷 Tokyo Culture Culture
